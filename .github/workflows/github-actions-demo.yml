name: C/C++ CI

on:
  [ push ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make ./exercise4/
    - name: test
      run: cd exercise4/test/ && g++ -o my_program main.cpp && sudo chmod g+x my_program && sudo ./my_program
